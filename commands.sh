# esto nos muestra todos los recursos que tiene la api de k8s que podemos usar, alias, versiones, kind
kubectl api-resources

# esto muestra solo las versiones de las apis que podemos usar de k8s
kubectl api-versions